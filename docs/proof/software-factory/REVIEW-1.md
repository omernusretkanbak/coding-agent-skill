# İNCELEME — Tur 1

PR #1 · base `94465a9` → head `735d54c` · İnceleyici: Fable 5.1 (taze bağlam, `Agent(model: fable)`) · Yazar: Sonnet 5 · Orkestratör: Fable 5.1 (oturum)

```
SKOR: 4
KARAR: DUZELTME_GEREKLI
INCELEYICI_MODEL: Fable 5.1 (claude-fable-5-1)
GUCLU_YANLAR:
- AGENTS.md:7-11 — dört adım, sabit roller (Sonnet yazar / Fable inceler / orkestratör ikisini de yapmaz), 0–5 döngü, 3 tur sınırı ve merge yasağı beş maddede eksiksiz; "mimari anlatmaz" maddesi kendini koruyacak şekilde yazılmış (madde 5). `wc -w` = 390 (≤400), CLAUDE.md `@AGENTS.md` importu + 7 satırlık Claude Code bloğu plana birebir uyuyor.
- .claude/settings.json:32-40 — geçerli JSON (ConvertFrom-Json OK); resmi permissions dokümanına göre deny > ask > allow sırası geçerli, `Bash(gh pr merge *)` tek sondaki-wildcard olduğundan çıplak `gh pr merge` ve `gh pr merge --auto` dahil tüm merge biçimlerini kapatıyor; `worktree.baseRef: "fresh"` EnterWorktree aracının belgelediği gerçek bir ayar.
- new-feature/SKILL.md:33-34 ve ship-it/SKILL.md:39-41 — Yasaklar tablolarındaki bahaneler scratchpad `red-results.md` / `red-results-r2-r3.md` transkriptleriyle kelimesi kelimesine örtüşüyor (writing-skills "Match the Form to the Failure" doğru uygulanmış; code-structure reçete, diğer üçü disiplin+tablo).
- docs/proof/software-factory/PROOF.md:72-79 — tüm ölçümler bağımsız yeniden üretildi: `wc -w` 390/429/389/367/482, `git check-ignore -v` iki eşleşme (.gitignore:2 ve :3), `file` → UTF-8/JSON, BOM yok, CRLF yok; `capture-screen.ps1 -Out <scratchpad>\review-capture.png` → exit 0, 203832 bayt, PNG imzası 89 50 4E 47, tam 1920x1080 masaüstü.
- PROOF.md:21-22, 47-48, 54 — R1/G1/G2 iddiaları klonlarda doğrulandı: `red-nf` main'de `8dce0f0` (RED gerçekten skill'siz main'e commit), `green-nf` main = `03ea962` + `.claude/worktrees/hello-script` `79904bc`, `green-pi` `b8f1e71` = hello.ps1 + test + `docs/proof/hello-script/PROOF.md`. `git diff --stat 03ea962..735d54c` yalnız PROOF.md → GREEN testleri incelenen skill içeriğinin birebir aynısı üzerinde koştu.
- Dört skill Claude Code 2.1.269'da gerçekten yükleniyor: worktree'den `claude -p` sondası dört adı ve "Use when…" açıklamalarını listeledi; `name` = klasör adı, description 354–392 karakter.
- builder-prompt.md:8 (yazar commit atmaz) ↔ prove-it/SKILL.md:38 (orkestratör commit'ler) ↔ ship-it/SKILL.md:17 (PROOF.md HEAD'de, ağaç temiz) — rol/sıra zinciri çelişkisiz; PROOF-template.md:24 "Orkestratör doğrulaması" bölümü builder-prompt.md:36 ile uyumlu (yazar boş bırakır).
- Güvenlik: commit'lenen 13 dosyada gizli bilgi, `C:\Users\…` yolu veya token yok (grep temiz); her iki commit mesajında Co-Authored-By var; `git status` temiz, dal `software-factory` origin'i izliyor.
ENGELLEYICI:
YOK
ONEMLI:
- .claude/skills/new-feature/SKILL.md:17 — "`--git-dir` ≠ `--git-common-dir` ise zaten izole bir worktree'desin, doğrudan Adım 2'ye geç" herhangi bir worktree'yi yeterli sayıyor; Çıkış Kapısı (satır 27) da yalnız "dal ≠ main VE cwd worktree içinde" istiyor — Plan "Worktree yerinde kalır" dediğinden, aynı oturumda ikinci bir görev geldiğinde tespit adımı önceki PR'ın worktree'sini kabul eder ve B görevinin commit'leri A dalına / açık PR A'ya karışır; AGENTS.md:7 "her görev kendi worktree'sinde ve dalında" kuralıyla çelişir — Tespit adımına slug karşılaştırması ekle: `git branch --show-current` görev slug'ına eşitse (ya da aynı PR'ın inceleme turuysa) Adım 2'ye geç, değilse `git worktree add ".claude/worktrees/<yeni-slug>" -b <yeni-slug> origin/main` + `EnterWorktree path=<mutlak yol>` ile yeni worktree aç (EnterWorktree `path` ile worktree içinden geçişi destekler; `name` ile yenisini açmaz).
- .claude/settings.json:32-40 (deny) ve :26-31 (ask) — resmi dokümana göre sondaki `*` yalnız kuralın tek wildcard'ıysa boş eşleşir; bu yüzden `Bash(git push * -f *)` `git push origin <dal> -f` biçimini YAKALAMAZ ve komut `Bash(git push *)` allow'una düşüp sessizce force-push olur; benzer şekilde `git push origin <dal>:refs/heads/main` ne `* main` ne `*:main` ask kuralına uyar, allow ile main'e fast-forward "merge" yapılabilir — PR'ın "force push deny" iddiasını ve merge yasağının mekanik ayağını deler — deny'a `Bash(git push * -f)` ve `Bash(git push * --force)` (+ PowerShell eşleri) ekle; ask'a boşluksuz `Bash(git push *main)` ekle (HEAD:main, x:refs/heads/main, --delete main hepsini yakalar; yanlış pozitif yalnız soru sorar); dokümanın belgelediği `git -C … push` / `git -c … push` atlamasına karşı `Bash(git -C *)`, `Bash(git -c *)` ask'a alınabilir.
KUCUK:
- .claude/skills/{new-feature,code-structure,prove-it,ship-it}/SKILL.md:3 — description düz YAML skaları içinde `Keywords:` (iki nokta + boşluk) var; PyYAML dört dosyada da "mapping values are not allowed here" veriyor. Claude Code'un kendi ayrıştırıcısı toleranslı (sonda doğrulandı) ama binary'deki skill-doctor metni "YAML frontmatter fails to parse → EVERY field dropped" diyor ve katı bir denetim/başka bir harness açıklamayı düşürür — kullanıcının diğer skill'leri gibi `description: >` katlanmış skalar kullan ya da çift tırnakla.
- .claude/skills/ship-it/reviewer-prompt.md:17 — "Her shell komutunu `cd "{WORKTREE_ABS}"; …` ile başlat": inceleyici alt-ajanın cwd'si zaten worktree'ye sabitlenir (bu incelemede öyleydi) ve orkestratör bu turda aynı biçimi kilit yüzünden reddedildiği için elle değiştirmek zorunda kaldı — cümleyi "cwd worktree'dir; git komutlarını tek başına çalıştır, gerekirse `git -C "{WORKTREE_ABS}"`" olarak yaz.
- .claude/settings.json:13 — `git stash *` allow'da; planın izin listesinde yok ve new-feature/SKILL.md:18 "bare `git stash` KULLANMA" diyor (stash yığını worktree'ler arası ortak) — ask'a taşı ya da kaldır.
- .claude/skills/new-feature/SKILL.md:17, 23, 42 — üç ayrı numaralandırma karışıyor: satır 17 "Adım 2" = hat adımı (code-structure), satır 23 "Adım 0/1" = superpowers using-git-worktrees adımları, satır 42 "Adım 3'ü (izolasyon)" = bu skill'in kendi 3. maddesi (hatta Adım 3 = KANITLA) — iç adımları harf/madde olarak adlandır, hat adımlarını "Hat Adım N" de.
- AGENTS.md:8 `<dal>` / AGENTS.md:47 `<dal-slug>` / ship-it/SKILL.md:17 `<dal-slug>` / ship-it/SKILL.md:25 `<slug>` / builder-prompt.md:36 `{DAL_SLUG}` — aynı dizin için dört farklı yer tutucu; docs/proof/README.md:11'deki tanımla tek ada (`<dal-slug>`) indir.
- .claude/skills/new-feature/SKILL.md:36-38 — üç satır, superpowers using-git-worktrees "Common Rationalizations" tablosunun (satır 163-165) çevirisi; satır 23 zaten o skill'e REQUIRED SUB-SKILL ile gönderiyor ve plan "kopyalanmaz, isimle referans verilir" diyor — yalnız RED'den gelen satırları bırak.
- .claude/skills/prove-it/scripts/capture-screen.ps1:20 — `$bitmap.Save($Out)` göreli yolu .NET süreç cwd'sine göre çözer, `Set-Location` sonrası oturum içinden çağrılırsa `$PWD`'den sapar (PROOF.md:113 göreli `-Out test.png` kullanıyor); ayrıca Save hata verirse Dispose çağrılmaz — başta `$Out = $ExecutionContext.SessionState.Path.GetUnresolvedProviderPathFromPSPath($Out)`, Save/Dispose'u try/finally'e al; yalnız birincil ekranı yakaladığını yorumda belirt.
- .claude/skills/ship-it/SKILL.md:21 ve prove-it/SKILL.md:38 — plan commit adımını (HEREDOC mesaj, Co-Authored-By, `--amend/--no-verify` yasağı) ship-it'e koymuştu; şimdi commit prove-it'te tek kelimeyle ("Orkestratör commit'ler") geçiyor, yasak ise commit'in olmadığı ship-it "Push" satırında duruyor — commit kurallarını prove-it "Kalıcılaştır" bölümüne taşı ya da ship-it'e "0. Commit" adımı ekle.
- docs/proof/software-factory/PROOF.md:5 — Aralık `94465a9..03ea962` yazıyor, PR head'i `735d54c`; fark yalnız bu dosya (doğrulandı) ama tabloda belirtilmemiş — "735d54c: yalnız PROOF.md" notu ekle.
GEREKCE: Dört adım, sabit roller, döngü ve merge yasağı gereksinimi karşılıyor ve PROOF.md'deki her ölçüm/klon kanıtı bağımsız olarak yeniden üretildi; ancak new-feature'ın "zaten worktree'deysen geç" mantığı ikinci görevde AGENTS.md madde 1'i sessizce bozuyor ve settings.json'daki deny/ask kalıpları `-f` sondaki ve `refs/heads/main` biçimlerini kaçırıyor, bu iki gerçek eksik teslimden önce kapatılmalı.
```

**Orkestratör kararı:** SKOR 4 < 5 → Döngü Kuralı: ENGELLEYİCİ (yok) + ÖNEMLİ (2) yeni Sonnet yazar görevine dönüştürüldü; ucuz KÜÇÜK maddeler de aynı tura alındı. Tur 2 → `code-structure` → `prove-it` (`## Tur 2`) → taze Fable inceleyici.
