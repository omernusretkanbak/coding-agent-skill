# İnceleme — Tur 1

| PR | Aralık (inceleyiciye verilen) | İnceleyici | Yazar |
|---|---|---|---|
| #3 | `4ed9a0f..1fbe815` | Fable 5.1 (taze bağlam, `Agent(model: fable)`) | Sonnet 5 |

```
SKOR: 4
KARAR: DUZELTME_GEREKLI
INCELEYICI_MODEL: Fable 5.1 (claude-fable-5-1)
GUCLU_YANLAR:
- `.claude/skills/prove-it/scripts/capture-page.mjs:608-616,531-545,684-691` — tek global `deadline` + etiketli `requireTime`, `--wait-ms` beklemeden ÖNCE kalan süre kontrolü; yeni "toplam süre sınırı" testi tabanda gerçekten kırmızı (PROOF: 12,7 sn/çıkış 0 → 7,2 sn/çıkış 1). Kendim koştum: `node --test capture-page.selftest.mjs` 13/13 PASS, çıkış 0, 61 sn; öncesi/sonrası `capture-page-|setInterval` süreç 0/0, `%TEMP%\capture-page*` dizini 0.
- `capture-page.mjs:794-805` (`isMainModule`, `realpathSync`) — junction testi gerçek `symlinkSync(...,'junction')` ile; regresyon yok: `--help` çıkış 0, argümansız çıkış 2, `--zoom-image`+`--url` çıkış 2 ("BİRLİKTE verilemez") — üçünü de kendim doğruladım.
- `capture-page.selftest.mjs:130-184,240-243,423-478` — A6 koşu-başına `runRoot` yalnız çağıran tarafta env override ile (betik değişmedi, `ownProfileDir.startsWith(runRoot)` assert'i bunu kanıtlıyor); izolasyon testi GERÇEK yabancı tarayıcı kullanıyor (benim koşumda da makine-geneli 16, kök-taramalı 0).
- `capture-page.selftest.mjs:490-500` — çakışma testi "Bilinmeyen parametre" ile yanlış geçmeye karşı korumalı; yazar zayıf RED'ini kendisi yakalayıp güçlendirmiş (scratchpad `b2/zoom-red-output.txt` PROOF'taki 2/2 FAIL ile birebir). `:502-564` zoom testi mock değil: gerçek PNG üretimi, IHDR 80x80, canvas `getImageData` ile piksel okuma.
- Kanıt–fixture tutarlılığı doğrulandı: `r/4` `refs/heads/gorev-silme = b19548f`, PROOF.md:30 "düğme metni üstü çizili DEĞİL … eşleşiyor" (yanlış iddia commit'li); `g/4` `68bb668`, PROOF.md:30-35 "5/5 açıldı", adıyla listeleme, `.zoom/tamamlandi-sil-zoom.png` kırpıntı, "EŞLEŞMEDİ", iddia 4 üstü çizili — PROOF'un GREEN (B) tablosuyla aynı. `vitest-host` RED/GREEN çıktı dosyaları PROOF ile birebir; vitest varsayılan deseni `**/*.{test,spec}.?(c|m)[jt]s?(x)` (`node_modules/vitest/dist/config.cjs:15`) `selftest.mjs`'i gerçekten kapsamıyor.
- Aracı gerçek vakada kendim çalıştırdım: `--zoom-image after-tamamlandi.png --region 20,60,160,32 --scale 4` → 640x128, "Sil" üzerindeki çizgi net; sınır `x+w==800` çıkış 0, `641,…` çıkış 2, `--scale 0/9` çıkış 2, eksik dosya çıkış 2; tüm denemelerimden sonra yetim süreç 0 (kapatılacak PID kalmadı).
- Kapsam: yalnız 7 dosya, AGENTS.md dokunulmamış, `wc -w` prove-it 500 / new-feature 496 / code-structure 390 / ship-it 492; `pilot-dersleri/PROOF.md` `+9/−0` ve taban satır 44 gerçekten "ölçüldü: orijinal PID ~birkaç ms" cümlesi; PR #3 açıklamasında `.claude/**` hariç tutma taşıma notu var (gereksinim 3). Diff üç geçişte okundu (docs/şablon; `capture-page.mjs`; eski test → yeni selftest blob-blob diff'i).
ENGELLEYICI:
- YOK
ONEMLI:
- `.claude/skills/prove-it/PROOF-template.md:33` ile `SKILL.md:23` ("scratch'e; adı PROOF'a") — Görseller yuvası kırpıntının yalnız DOSYA ADINI istiyor, `--region x,y,w,h --scale n` değerlerini istemiyor; kırpıntı da scratch'te kalıyor. Sonuç: "eşleşti/eşleşmedi" kararının dayanağı ne docs/proof altında yaşıyor ne yeniden üretilebiliyor (G4′ fixture'ı bunu gösteriyor: `g/4` PROOF.md:35 `.zoom/tamamlandi-sil-zoom.png` yazıyor, `.zoom\` silinmiş, bölge yok — inceleyici aynı bölgeyi kesemez). Bu, AGENTS.md kural 2 ("kanıt docs/proof altında yaşar") ve prove-it "Yeniden üretme" ilkesine aykırı; PR'ın kendi hedefi (yüzeysel bakış yerine denetlenebilir kırpıntı) yarım kalıyor. Düzeltme: yuvayı `{kırpıntı.png (--region x,y,w,h --scale n) → gözlem → eşleşti/eşleşmedi}` yap; `SKILL.md:23`'te "adı PROOF'a" → "adı/bölgesi PROOF'a" (kelime sayısı korunur). Alternatif: kırpıntıyı PROOF.md yanına yaz.
- `capture-page.mjs:259-277` (`buildZoomDataUrl`, çift base64 `data:text/html;base64,…`) — kaynak PNG ~1,2 MB'ı aşınca data URL Chromium'un 2 MB URL sınırını (`kMaxURLChars`) geçiyor ve `Page.navigate` düşüyor. Ölçtüm: 1.004.894 B kaynak → URL ~1,79 MB → çıkış 0; 1.443.328 B kaynak → URL ~2,57 MB → `Hata: Sayfa yüklenemedi: net::ERR_ABORTED (data:text/html;base64,PCFkb2…)` çıkış 1 ve `:661` `${args.url}` yüzünden stderr'e MEGABAYT boyunda URL dökülüyor. Tam sayfa/görsel yoğun `after.png`'ler bu sınıfa girer; hata iletisi kök nedeni (boyut) söylemiyor. Düzeltme: HTML'i `run()` içinde `profileDir`'e (zaten koşuya özgü, temizlenen dizin) `zoom.html` olarak yazıp `file://` ile gez, ya da `about:blank` sonrası `Page.setDocumentContent`; her durumda `:661`'de `data:` URL'leri kısalt (ilk 80 karakter).
KUCUK:
- `capture-page.mjs:247-251` (`pngDims`) + `:283-292` (`prepareZoom`) — PNG imzası/IHDR kontrolü yok. Denedim: 100+ baytlık metin dosyası `--region`siz → tarayıcı başlatılıp CDP `Invalid parameters params.width` ile çıkış 1; `--region 0,0,10,10` ile → çıkış 0, 40x40 "kırık görsel" simgesi (SAHTE BAŞARI); 3 baytlık dosya → RangeError metniyle çıkış 2. 8 baytlık imza + `IHDR` kontrolüyle tarayıcı açılmadan çıkış 2 ver.
- `capture-page.mjs:200-205,743-756` — `--region`/`--scale`, `--url` moduyla verildiğinde sessizce yutuluyor (denedim: `--url about:blank --region 0,0,10,10 --scale 2` → çıkış 0, 50x40). `args.zoomImage` yokken bu bayraklar kullanım hatası (çıkış 2) olmalı.
- `capture-page.selftest.mjs:544-556` — yalnız MERKEZ piksel kırmızı kontrol ediliyor; `left/top` işaret ya da ±birkaç piksel kayması bu asserti geçer. Bölge tam kareye eşit olduğu için `(0,0)` ve `(79,79)` köşeleri de `[255,0,0,255]` olmalı — iki köşe ekle.
- `capture-page.mjs:673-675` — `reloadEventPromise`'a `:657`'deki gibi `.catch(() => {})` bağlanmamış; `Page.reload` yanıtı ve `loadEventFired` aynı deadline'da birlikte zaman aşımına düşerse ikinci reddi `unhandledRejection` olur, süreç `cleanupResources` bitmeden ölür (yetim riski). Önceden de vardı; A3 iki zamanlayıcıyı aynı bitiş anına bağladığı için görünürlüğü arttı.
- `.claude/skills/prove-it/SKILL.md:43` (Kalıcılaştır) — sıkıştırmada "Orkestratör commit'ler" öznesi düştü ("commit mesajı `-F` ile … ekler" öznesiz). Kim commit'ler bilgisi artık yalnız builder-prompt'ta örtük. Özneyi geri koy; dosya 500/500 kelimede olduğundan başka bir yerden 2 kelime kırp.
GEREKCE: A1–A6 ve C gerçek RED/GREEN'le kapanmış, 13/13 test ile 0/0 yetim benim koşumda da tutuyor, R4/G4′ fixture'ları PROOF anlatısını birebir doğruluyor; ancak yeni yuva kırpıntının bölgesini kayıt altına almadığı için "eşleşti" kararı yeniden üretilemez kalıyor ve `--zoom-image` ~1,2 MB üzeri kaynak PNG'de 2 MB data-URL sınırına takılıp yanıltıcı `ERR_ABORTED` veriyor — ikisi de ucuz düzeltmeli ama gerçek eksikler.
```

## Orkestratör kararı

SKOR 4 < 5 → Tur 2. Yeni Sonnet yazar görevi: ONEMLI-1 (yuva ve SKILL.md kırpıntının `--region/--scale` değerlerini kayda geçirsin → yeniden üretilebilir) ve ONEMLI-2 (`--zoom-image` HTML'i `profileDir`'e `zoom.html` olarak yazılıp `file://` ile gezilsin; hata iletisinde `data:` URL'leri kısaltılsın; büyük PNG için regresyon testi) zorunlu; KUCUK 1–5'in hepsi de bu tura alınır. ONEMLI-1 skill metnini değiştirdiği için Tur 2'de G4 senaryosu regresyon olarak yeniden koşulur (bölge kaydının yazıldığı doğrulanır).
