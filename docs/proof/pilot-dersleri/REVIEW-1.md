# İnceleme — Tur 1

| PR | Aralık (inceleyiciye verilen) | İnceleyici | Yazar |
|---|---|---|---|
| #2 | `16fcf2c..9371217` | Fable 5.1 (taze bağlam, `Agent(model: fable)`) | Sonnet 5 |

```
SKOR: 4
KARAR: DUZELTME_GEREKLI
INCELEYICI_MODEL: Fable 5.1 (claude-fable-5-1)
GUCLU_YANLAR:
- `.claude/skills/prove-it/scripts/capture-page.mjs:281,207-220` — `--remote-debugging-port=0` + `DevToolsActivePort` okuma: sabit port yok, chrome-devtools MCP'nin profil/port kilidiyle çakışmıyor; pilotun gerçek hatasını ("already running … --isolated") kökten atlıyor.
- `.claude/skills/prove-it/scripts/capture-page.mjs:293-315,411-423,520-525` — tek idempotent `cleanupResources` + `finally` + SIGINT/SIGTERM; başarı/hata/iç zaman aşımı yollarının hepsi aynı temizlikten geçiyor. Bunu kendim doğruladım: `node --test` 7/7 PASS, çıkış 0 (40,5 sn); öncesi/sonrası tarayıcı süreci 0/0, `%TEMP%\capture-page-*` dizini 0/0.
- `.claude/skills/prove-it/scripts/capture-page.test.mjs:175-257` — testler mock değil gerçek davranış: gerçek headless Edge + gerçek `node:http`, PNG imzası + IHDR boyutu, `--media` etkisini `getComputedStyle` ile, `--setup` etkisini reload sonrası `document.title` ile, yetim süreç/profil/CDP portunu üç ayrı çıkış yolunda doğruluyor.
- `.claude/skills/prove-it/PROOF-template.md:12,32,43` + `SKILL.md:23,43` — göreli Aralık, zorunlu Aralık yuvası, zorunlu port kontrolü (somut `Get-NetTCPConnection` komutu, yalnız PID ile durdurma). Fixture'larda doğruladım: G3′ (`g\3`) Aralık `1caca05..bu turun commit'i`, commit `82a05c2` reflog'da var; G3b `5818cfe..`, PID 42308; G2 Tur 2 `c73e52f..`, tek commit `d5d659c`; R2 `1a199bb..51da48d` reflog'la uyumlu; R3/R3b PROOF'larda "çalışma ağacı (henüz commit'lenmedi)" gerçekten var; G1 `capture-page.mjs` ile 1000x700, PID 57376 `taskkill /PID`; `evidence\red-subject-real-capture-screen.ps1.txt` gerçekten `CopyFromScreen` kullanıyor, R1 PROOF'unda Claude Code penceresinin yakalandığı notu var.
- Kapsam disiplini: diff yalnız 8 dosya; AGENTS.md dokunulmamış (kural 5); `capture-screen.ps1` diff'i yalnız başlık yorumu (gövde/param aynı); `wc -w` ölçtüm: 390 / 496 / 500 / 492 (hepsi ≤ 500). `ornek-capture.png` IHDR 640x400, 8590 B, açıp baktım: yalnız sayfa, masaüstü yok. `--help` çıkış 0, argümansız çıkış 2.
ENGELLEYICI:
- YOK
ONEMLI:
- `.claude/skills/prove-it/scripts/capture-page.mjs:258-267` — `killByProfileDir` Windows dalı yalnız `CommandLine -like '*<profileDir>*'` ile filtreliyor, ad/PID dışlaması yok; sorguyu çalıştıran `powershell.exe`'nin kendi komut satırında profil yolu geçtiği için KENDİSİYLE eşleşip `Stop-Process -Id <kendi PID> -Force` ile her geçişte (3×/koşu) kendini öldürüyor — Kendim çoğalttım (betiğin komutunu birebir taklit eden scratchpad tanısı): liste geçişinde `58552 powershell.exe` / `self=58552` eşleşti ve ondan SONRA iki profil taşıyan süreç (`34512 msedge.exe`, `36272 identity_helper.exe`) sıralandı; kill geçişi `killing 59320 powershell.exe` ile bitip çıkış 4294967295 (−1) döndü. Bugün "çalışıyor" olmasının tek nedeni Win32_Process'in oluşturulma sırasıyla dönmesi ve önce kök tarayıcının öldürülüp ağacı düşürmesi — söylenmemiş bir bağımlılık; üstelik test dosyası aynı yanlış pozitifi açıkça belgeleyip (`capture-page.test.mjs:49-53`) ad filtresi koyuyor, betik koymuyor. Bu PR'ın ana konusu yetim süreçler olduğu için çekirdek mekanizmanın kazara çalışması önemli — `Where-Object { $_.ProcessId -ne $PID -and $_.CommandLine -like '*…*' }` ekle (yardımcı süreçleri kapsamak için CommandLine eşleşmesini koru, yalnız kendi PID'ini dışla); POSIX `pkill -f` zaten kendini dışlar, dokunma.
- `.claude/skills/prove-it/scripts/capture-page.test.mjs:143-145` (ve 177,189,200,212,225,241) — düzenek alt süreci 20000 ms'de `child.kill()` ile öldürüyor, ama testler `--timeout-ms` vermediğinden betik 30000 ms varsayılanıyla (`capture-page.mjs:99`) çalışıyor; temizliğin kendisi bile ~18 sn'ye çıkabiliyor (`waitForExit` 1000 + 2×300 + 3 PowerShell + `removeProfileDir` 15000, satır 304-314/325) — Betiğin kendi sözleşmesini (`capture-page.mjs:33-36,62-64`: "dış zaman aşımı iç olandan uzun olmalı") kendi test dosyası çiğniyor; Windows'ta `child.kill()` = TerminateProcess → `finally`/sinyal işleyici çalışmaz → PROOF.md:43'te 110 yetim `msedge.exe`'nin nedeni olarak gösterilen mekanizma aynen canlı kalıyor — `runCapture` `--timeout-ms` verilmemişse `--timeout-ms 8000` eklesin ve düzenek zaman aşımı ≥ iç zaman aşımı + temizlik bütçesi (ör. 45000) olsun; ya da düzenek zaman aşımını betiğin `--timeout-ms` + 20 sn'den türet.
KUCUK:
- `.claude/skills/prove-it/scripts/capture-page.mjs:23-26` ve `docs/proof/pilot-dersleri/PROOF.md:44` — "Edge kendini ERKEN EVREDE YENİDEN BAŞLATIR, spawn PID saniyenin altında çıkar" kesinlik diliyle "ölçüldü" deniyor; aynı argümanlarla yaptığım çoğaltmada spawn PID 36920 kök tarayıcı olarak 5,2 sn boyunca hayatta kaldı ve ancak `Stop-Process` ile çıktı. Strateji (profil-dizini taraması) yine de doğru; yorumu "yeniden başlatabilir / PID'e güvenilmez" diye yumuşat.
- `.claude/skills/prove-it/scripts/capture-page.mjs:260-262` — `-like '*${escaped}*'` yalnız tek tırnağı kaçırıyor; `profileDir` içinde PowerShell joker karakteri (`[`, `]`, `*`, `?` — köşeli parantezli kullanıcı adı/TEMP) varsa eşleşme sessizce boş döner ve yetim kalır; `[WildcardPattern]::Escape()` ya da `$_.CommandLine.Contains('…')` kullan.
- `.claude/skills/prove-it/scripts/capture-page.test.mjs:103,121-127` ile `PROOF.md:45` — "before/after 0 → 0 zorunlu kılar" deniyor ama `processesBefore` yalnız mesajda; önceden kalmış bir yetim süite yanlış nedenle düşürür. Ya `before()`'da `assert.equal(processesBefore, 0)` ekle ya da PROOF ifadesini "yalnız sonrası 0" olarak düzelt.
- `.claude/skills/prove-it/scripts/capture-page.mjs:504` — ifade `undefined` dönerse `JSON.stringify(undefined)` → `EVAL=undefined` (JSON değil); `?? null` ile normalize et.
- `.claude/skills/prove-it/scripts/capture-page.mjs:309-313` — başarı yolunda `Browser.close` sonrası da 3 PowerShell geçişi + 600 ms; her yakalamaya ~2 sn ekliyor. Önce sayım yapıp 0 ise geçişleri atla (performans).
- `.claude/skills/prove-it/SKILL.md` tam 500 kelime — bir sonraki düzenlemede sıfır pay var; bilgi notu.
- `docs/proof/pilot-dersleri/PROOF.md:29,128` — R1 öznesinin `Stop-Process -Name msedge -Force` çalıştırdığı iddiası korunan kanıtta (`evidence\red-s1-PROOF.md`, iki `.ps1.txt`) yok; orkestratör transkript gözlemi olarak işaretle. Yük taşıyan iddia değil.
GEREKCE: Üç gereksinim de karşılanmış, kanıt iddiaları (7/7 test, 0/0 yetim, kelime sayıları, fixture Aralık/PID/commit değerleri) bağımsız çoğaltmamla tutarlı; ancak yetim-süreç önlemenin çekirdeği olan `killByProfileDir` kendi kabuk sürecini öldürüp belgelenmemiş enumerasyon sırasına dayanıyor ve test düzeneği betiğin kendi zaman aşımı sözleşmesini çiğneyerek PROOF'un bizzat teşhis ettiği yetim mekanizmasını canlı bırakıyor — ikisi de 5 için düzeltilmeli.
```

## Orkestratör kararı

SKOR 4 < 5 → Tur 2. Yeni Sonnet yazar görevi: ONEMLI-1 (`killByProfileDir` kendi PID'ini dışlasın) ve ONEMLI-2 (test düzeneği zaman aşımı ≥ iç zaman aşımı + temizlik bütçesi) zorunlu; KUCUK-1 (yorum dili), KUCUK-2 (joker kaçışı), KUCUK-3 (`before()` assert), KUCUK-4 (`EVAL` `null`), KUCUK-5 (0 ise geçişleri atla), KUCUK-7 (`Stop-Process -Name` iddiasını orkestratör gözlemi olarak işaretle) de bu tura alınır. KUCUK-6 bilgi notu; eylem yok. Tur 2 kanıtı PROOF.md'ye `## Tur 2` olarak eklenir.
